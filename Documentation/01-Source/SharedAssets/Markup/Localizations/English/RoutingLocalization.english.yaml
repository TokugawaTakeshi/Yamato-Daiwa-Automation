fileNameLocalizationExtensions: english

topPage:

  $heading: "Top Page"
  $URI: @All/index

terminology:

  $heading: "Terminology"

functionality:

  $heading: "Functionality"

  markupProcessing:

    $heading: "Markup Processing"

    basicPugToHTML_Transpiling:

      $heading: "Basic Pug-to-HTML Transpiling"

    resourcesPointersResolving:

      $heading: "Resources Pointers Resolving"

  browserLiveReloading:

    $heading: "Browser Live Reloading"

  distributablePackageJSON_Generating:

    $heading: "Output package.json Generating"

configuration:

  $heading: "Configuration"

  common:

    $heading: "<code>$commonSettings</code> — Common Settings"

  markupProcessing:

    $heading: "<code>$markupProcessing</code> — Markup Processing Settings"

  stylesProcessing:

    $heading: "<code>$stylesProcessing</code> — Styles Processing Settings"

  ECMAScriptLogicProcessing:

    $heading: "<code>$ECMAScriptLogicProcessing</code> — ECMAScript Logic Processing Settings"

  imagesProcessing:

    $heading: "<code>$imagesProcessing</code> — Images Processing Settings"

  browserLiveReloading:

    $heading: "<code>$browserLiveReloading</code> — Browser Live Reloading Settings"

  outputPackageJSON_Generating:

    $heading: "Output package.json Generating"
