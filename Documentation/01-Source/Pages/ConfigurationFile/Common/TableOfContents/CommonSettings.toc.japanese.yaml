selectiveExecutions:

  $heading: "<code>$selectiveExecutions</code> 選択的実行"

  settingUpOfEachItem:

    $heading: "選択的実行毎設定"

    tasksAndSourceFilesSelection:

      $heading: "<code>$tasksAndSourceFilesSelection</code> 課題・ソースファイルの選択"

    browserLiveReloadingSetupID:

      $heading: "<code>$browserLiveReloadingSetupID</code> ブラウザータブ自動リロード設定ID"

    dockerSetupID:

      $heading: "<code>$dockerSetupID</code> Docker設定のID"

    distributablePackageJSON_Generating:

      $heading: "<code>$distributablePackageJSON_Generating</code> 成果物専用「package.json」ファイルの生成"

    processingOnDemand:

      $heading: "<code>$processingOnDemand</code>　需要に応じて処理"

      enable:

        $heading: "<code>enable</code> 有効化するべきか"

      fullInitialBuilding:

        $heading: "<code>fullInitialBuilding</code>　完全な初期構成が必要か"


publicDirectoriesRelativePaths:

  $heading: "<code>$publicDirectoriesRelativePaths</code> 構成モード毎パブリックディジタルの早退パス"

filesWatching:

  $heading: "<code>$filesWatching</code> ファイル監視"

  excludeFilesWithPathsRelativeToProjectRootDirectory:

    $heading: "<code>excludeFilesWithPathsRelativeToProjectRootDirectory</code> 除外ファイルの相対パス"

  excludeDirectoriesWithPathsRelativeToProjectRootDirectory:

    $heading: "<code>excludeDirectoriesWithPathsRelativeToProjectRootDirectory</code> 除外ディレクトリの相対パス"

  buildingModeDependent:

    $heading: "<code>buildingModeDependent</code> 構成モード毎"

    excludeFilesWithPathsRelativeToProjectRootDirectory:

      $heading: "<code>excludeFilesWithPathsRelativeToProjectRootDirectory</code> 除外ファイルの相対パス"

    excludeDirectoriesWithPathsRelativeToProjectRootDirectory:

      $heading: "<code>excludeDirectoriesWithPathsRelativeToProjectRootDirectory</code> 除外ディレクトリの相対パス"
