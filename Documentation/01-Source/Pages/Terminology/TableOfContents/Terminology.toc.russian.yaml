project:

  $heading: Проект
  building:

    $heading: Сборка проекта

task: { $heading: "Задача (Task)" }

scenario: { $heading: "Сценарий (Scenario)" }

projectBuildingMode: { $heading: "Режим сборки проекта (Project building mode)" }

entryPointsAndChildrenFiles:

  $heading: "Точки входа и дочерние файлы (Entry points / children files)"

  byFileType:

    $heading: "По типу файлов"

    markup: { $heading: "Разметка" }
    styles: { $heading: "Стили" }
    ECMA_ScriptLogic: { $heading: "ECMAScript-логика" }

  entryPointsGroup: "Группа точек входа (Entry points group)"

assets:

  $heading: "Ассеты (Assets)"

  groups:

    $heading: "Группы ассетов (Assets groups)"

resources:

  $heading: "Ресурсы (Resources)"

selectiveExecution:

  $heading: "Выборочное выполнение (Selective execution)"

importantForYDA_ExistingTerms:

  $heading: "Важный для YDA общие термины"

  publicPathAndShortenedAbsolutePath:

    $heading: "Публичный путь и укороченный абсолютный путь (Public path / Shortened absolute path)"
